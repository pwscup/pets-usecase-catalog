# Task 012: フィルタ状態のURL同期

## フェーズ
Phase 2: 検索・フィルタ・ソート機能

## 概要
フィルタ・検索状態をURLハッシュパラメータに反映し、ブックマーク/共有可能にする。

## 作業内容
1. フィルタ状態をURLのクエリパラメータに同期（例: `/#/?q=金融&region=国内`）
2. URL からフィルタ状態を復元
3. ブラウザの戻る/進むボタンでフィルタ状態が復元される

## 完了条件
- フィルタ操作でURLが更新される
- URLを直接入力してフィルタ済みの一覧が表示される
